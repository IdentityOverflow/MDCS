# Docker Environment Configuration Template for MDCS
# Copy this file to .env and customize the values

# ============================================
# Database Configuration
# ============================================
DB_NAME=mdcs
DB_USER=mdcs
DB_PASSWORD=changeme_use_strong_password_here

# ============================================
# Backend Configuration
# ============================================
# Database URL is auto-constructed from above values
# Format: postgresql://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}

# ============================================
# Ollama Configuration (for host connectivity)
# ============================================
# If running Ollama on host machine:
# - Backend can access via: http://host.docker.internal:11434
# - Frontend users configure in UI settings: http://localhost:11434
#
# If running Ollama in Docker:
# - Add Ollama service to docker-compose.yml
# - Both containers use service name: http://ollama:11434

# ============================================
# OpenAI Configuration
# ============================================
# OpenAI API keys are configured in frontend UI settings
# No backend configuration needed

# ============================================
# Deployment Notes
# ============================================
# 1. Copy this file: cp .env.docker .env
# 2. Change DB_PASSWORD to a strong password
# 3. Run: docker compose up -d
# 4. Access frontend: http://localhost:5173
# 5. Access backend API docs: http://localhost:8000/docs
